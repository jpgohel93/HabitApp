<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:background="@drawable/card_default"
    android:padding="12dp"
    android:id="@+id/my_profile_option_layout"
    android:layout_marginBottom="@dimen/app_vertical_margin"
    android:layout_height="wrap_content">

    <ImageView
        android:layout_width="60dp"
        android:layout_gravity="center"
        android:id="@+id/opIcon"
        android:src="@drawable/share_icon"
        android:background="@drawable/card_light"
        android:padding="16dp"
        android:layout_height="60dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:text="@string/option_share"
        android:id="@+id/opText"
        android:layout_marginStart="@dimen/backup_card_padding"
        android:textSize="@dimen/root_title_text"
        android:textStyle="bold"
        android:layout_gravity="center"
        android:layout_height="wrap_content"/>
</LinearLayout>